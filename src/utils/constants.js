export const colors = {
  red: 'red',
  yellow: 'yellow',
  blue: 'blue',
  green: 'green',
  purple: 'purple',
  brown: 'brown',
  lightblue: 'lightblue',
  burlywood: 'burlywood',
};

export const easyGame = Object.keys(colors).slice(0, 5);
export const mediumGame = Object.keys(colors).slice(0, 6);
export const hardGame = Object.keys(colors).slice(0, 7);
export const extraHardGame = Object.keys(colors);
